// v1.0
if (!["mwak","rwak"].includes(args[0].item.data.actionType)){
    console.log("Type of Attack: ", args[0].item.data.actionType);
    return {};
}  

console.log("You made a melee or ranged attack");

actor = token.actor;
const barbarianLevels = actor.getRollData().classes.barbarian?.levels;

console.log("Barbarian Levels:", barbarianLevels);

// How to check that we've already done one this turn?
return {damageRoll: `${baseDice * diceMult}d6 + ${barbarianLevels/2}`, flavor: "Divine Fury"};